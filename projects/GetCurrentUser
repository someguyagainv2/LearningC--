#include <iostream>
#include <windows.h>
#include <Lmcons.h> 

std::wofstream fileW;

int main() {
    wchar_t username[UNLEN + 1]; 
    DWORD username_len = UNLEN + 1;

    GetUserNameW(username, &username_len);

    return 0;
}
